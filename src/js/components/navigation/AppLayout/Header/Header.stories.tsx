import React, {useState} from 'react';
import Header from './Header'
import {FOLDER_APP} from '../../../../consts/storyBook';

export default {
  title: `${FOLDER_APP}/Header`
};


export const Header_ = () => {

  return (
    <Header />
  )
}